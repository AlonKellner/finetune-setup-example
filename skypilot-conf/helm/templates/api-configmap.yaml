{{- if .Values.skypilot.apiService.customConfig }}
apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ .Release.Name }}-config
  namespace: {{ .Release.Namespace }}
data:
  config.yaml: |
{{ .Values.skypilot.apiService.customConfig | toYaml | indent 4 }}
{{- end }}
